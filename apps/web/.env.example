# ======================================================================
# In and Out Car Wash - Web Application Environment Configuration
# ======================================================================

# API Configuration
# -----------------
# The base URL for the backend API
# For local development: http://localhost:3000/api
# For network access (other devices): http://YOUR_IP:3000/api
# Example: http://192.168.1.100:3000/api
VITE_API_URL=http://localhost:3000/api

# Payment Integration
# ------------------
# Stripe publishable key for payment processing
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# WhatsApp Integration
# --------------------
# WhatsApp business number for booking confirmations
VITE_WHATSAPP_NUMBER=9710554995611

# Application Settings
# --------------------
# Enable demo mode for testing (true/false)
# WARNING: Only enable in development, never in production
VITE_DEMO_MODE=false

# ======================================================================
# SECURITY CONFIGURATION
# ======================================================================

# Session Signing Secret
# ----------------------
# Used for HMAC signing of session tokens to prevent tampering
# Generate a secure random string (min 32 characters)
# Example: openssl rand -hex 32
VITE_SESSION_SECRET=your-secure-random-session-secret-min-32-chars

# Manager Authentication
# ----------------------
# Manager email and PASSWORD HASH (not plaintext password!)
# To generate the password hash:
# 1. Open browser console on the running app
# 2. Run: await import('./utils/secureSession').then(m => m.hashPassword('YourPassword', 'manager@example.com'))
# 3. Copy the resulting hash here
VITE_MANAGER_EMAIL=manager@3on.ae
VITE_MANAGER_PASSWORD_HASH=your-sha256-password-hash-here

# Staff Authentication
# --------------------
# Staff credentials as JSON array with PASSWORD HASHES (not plaintext!)
# Format: [{"email":"staff@3on.ae","passwordHash":"<hash>","name":"Staff Name"}]
# To generate password hashes, use the same method as manager above:
# await import('./utils/secureSession').then(m => m.hashPassword('Password', 'email@example.com'))
VITE_STAFF_CREDENTIALS='[{"email":"car1@3on.ae","passwordHash":"your-hash-here","name":"Vehicle 1"},{"email":"car2@3on.ae","passwordHash":"your-hash-here","name":"Vehicle 2"}]'

# ======================================================================
# Firebase Configuration
# ======================================================================
# Firebase credentials for authentication and database
# Get these from: https://console.firebase.google.com/ > Project Settings
#
# SECURITY NOTE: Firebase API keys are meant to be public (they identify
# your project). Security is enforced through Firebase Security Rules.
# Ensure your firestore.rules are properly configured!

VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# IMPORTANT: For Phone Authentication (reCAPTCHA) to work:
# Add your domain to Firebase Console > Authentication > Settings > Authorized domains
# Required domains:
#   - localhost (for development)
#   - Your production domain
#   - Any staging/preview URLs

# ======================================================================
# How to use this file:
# ======================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values with your actual configuration
# 3. IMPORTANT: Generate secure password hashes (see instructions above)
# 4. IMPORTANT: Generate a secure session secret
# 5. To access from other devices on your network:
#    - Find your IP: Run 'ipconfig' (Windows) or 'ifconfig' (Mac/Linux)
#    - Update VITE_API_URL with your IP address
#    - Example: VITE_API_URL=http://192.168.1.100:3000/api
# 6. Restart the dev server after changing .env values
#
# SECURITY CHECKLIST:
# [ ] Session secret is at least 32 characters
# [ ] Manager password is hashed (not plaintext)
# [ ] Staff passwords are hashed (not plaintext)
# [ ] DEMO_MODE is false in production
# [ ] Firebase security rules are deployed
# [ ] .env file is in .gitignore (never commit secrets!)
