# ======================================================================
# 3ON Mobile Carwash - Web Application Environment Configuration
# ======================================================================
# Copy this file to .env.local: cp .env.example .env.local
# NEVER commit .env.local or any file with real credentials!

# ======================================================================
# API Configuration
# ======================================================================
# Backend API URL
# Local development: http://localhost:3000/api
# Network access: http://YOUR_IP:3000/api (for mobile testing)
VITE_API_URL=http://localhost:3000/api

# ======================================================================
# Payment Integration
# ======================================================================
# Stripe publishable key (safe for client-side, but keep in env for flexibility)
# Get from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# ======================================================================
# WhatsApp Integration
# ======================================================================
# WhatsApp Business number for booking confirmations (with country code, no +)
VITE_WHATSAPP_NUMBER=971XXXXXXXXX

# ======================================================================
# Application Settings
# ======================================================================
# Demo mode for testing - NEVER enable in production
# Only works in development mode as additional safeguard
VITE_DEMO_MODE=false

# ======================================================================
# Firebase Configuration
# ======================================================================
# Get these from: Firebase Console > Project Settings > General > Your apps
#
# SECURITY NOTE: Firebase client-side API keys are designed to be public.
# They identify your project but don't grant access - security is enforced
# through Firebase Security Rules. Still, keeping them in env vars:
# 1. Makes it easy to switch between dev/staging/prod projects
# 2. Prevents accidental use of production credentials in development
# 3. Follows industry best practices

VITE_FIREBASE_API_KEY=AIzaSy_your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxx
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ======================================================================
# Staff & Manager Authentication
# ======================================================================
# Authentication is handled via Firebase Auth with custom claims.
# Staff and Manager accounts are created in Firebase Console with:
#   - Email/password authentication
#   - Custom claim: { role: "staff" } or { role: "manager" }
#
# These env vars are used for email validation only (optional):
VITE_STAFF_EMAILS=car1@example.com,car2@example.com,car3@example.com
VITE_MANAGER_EMAIL=manager@example.com

# Owner notification email (used by Cloud Functions, not exposed to client)
VITE_OWNER_NOTIFICATION_EMAIL=owner@example.com

# ======================================================================
# Setup Instructions
# ======================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Get Firebase credentials from console.firebase.google.com
# 3. Create staff/manager accounts in Firebase Authentication
# 4. Set custom claims using Firebase Admin SDK or Cloud Functions:
#    admin.auth().setCustomUserClaims(uid, { role: 'staff' })
# 5. Deploy Firebase Security Rules (firestore.rules)
# 6. For phone auth: Add your domain to Firebase Auth > Authorized domains
#
# Development Tips:
# - Use .env.local for local development (gitignored)
# - Use .env.development for shared dev settings (optional)
# - For Vercel: Add env vars in Project Settings > Environment Variables
