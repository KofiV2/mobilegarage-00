/**
 * Re-export from shared package
 * All package configuration is now in @3on/shared
 */
import {
  DEFAULT_ADDONS,
  VEHICLE_TYPES,
  VEHICLE_SIZES,
  PACKAGES,
  PACKAGES_LIST,
  getPackagePrice,
  vehicleRequiresSize,
} from '@3on/shared';

export {
  DEFAULT_ADDONS,
  VEHICLE_TYPES,
  VEHICLE_SIZES,
  PACKAGES,
  PACKAGES_LIST,
  getPackagePrice,
  vehicleRequiresSize,
};

export default PACKAGES;
